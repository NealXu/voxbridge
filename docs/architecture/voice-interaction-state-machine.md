# 语音交互状态机

**最后更新**：2026-08-16

## 状态图

```
┌─────────────────────────────────────────────────────────────────────┐
│                           [启动]                                     │
│                              │                                       │
│                              ▼                                       │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │                      READY (就绪)                               │ │
│  │  显示: "就绪，按 F9 说话（Ctrl+C 退出）"                         │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                              │                                       │
│                        [按 F9]                                        │
│                              │                                       │
│                              ▼                                       │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │                    RECORDING (录音中)                           │ │
│  │  显示: "🎙 录音中…（松开结束 / Esc 取消）"                        │ │
│  └────────────────────────────────────────────────────────────────┘ │
│           │                              │                           │
│     [按 F9/松开]                    [按 Esc]                        │
│           │                              │                           │
│           ▼                              ▼                           │
│  ┌──────────────────────┐    ┌─────────────────────────────────┐   │
│  │  PROCESSING (处理中)  │    │  CANCELLING (取消中)             │   │
│  │  显示:               │    │  显示: "已取消"                  │   │
│  │  "语音输入成功，     │    │  (500ms 后)                      │   │
│  │   识别中..."         │    │  显示: "就绪，按 F9 说话..."     │   │
│  └──────────────────────┘    └─────────────────────────────────┘   │
│           │                              │                           │
│           ▼                              └───────────┬───────────►──┤
│  ┌──────────────────────┐                             │              │
│  │  EDITING (编辑确认)   │                             │              │
│  │  显示:               │                             │              │
│  │  🎤 {识别文本}       │                             │              │
│  │  (Enter 发送 / ...   │                             │              │
│  └──────────────────────┘                             │              │
│     │            │                                     │              │
│ [Enter]      [Esc]                                     │              │
│     │            │                                     │              │
│     ▼            ▼                                     │              │
│  ┌────────┐  ┌─────────────────────────────────────┐  │              │
│  │SENDING │  │  CANCELLED_EDIT (编辑取消)           │  │              │
│  │(发送中)│  │  显示: "已取消"                      │  │              │
│  └────────┘  │  (短暂停留后)                        │  │              │
│     │        │  显示: "就绪，按 F9 说话..."         │  │              │
│     │        └─────────────────────────────────────┘  │              │
│     │            │                                     │              │
│     ▼            └─────────────────────────────────────┘              │
│  ┌────────────────────────────────────────────────────────────────┐   │
│  │                    AGENT_RUNNING (Agent 执行)                   │   │
│  │  显示: 流式输出 / 工具调用 / 成本统计                            │   │
│  └────────────────────────────────────────────────────────────────┘   │
│     │                                                                 │
│     ▼                                                                 │
│     └───────────────────────────────────────────────────────────────►│
│                                                                       │
│                          (回到 READY)                                 │
└─────────────────────────────────────────────────────────────────────┘
```

## 状态定义

| 状态 | 触发事件 | 显示内容 | 后续状态 |
|------|---------|---------|---------|
| READY | F9 按下 | "就绪，按 F9 说话（Ctrl+C 退出）" | RECORDING |
| RECORDING | F9 释放 | "🎙 录音中…（松开结束 / Esc 取消）" | PROCESSING |
| RECORDING | Esc | - | CANCELLING |
| PROCESSING | STT 完成 | "语音输入成功，识别中…" | EDITING |
| PROCESSING | 超时/错误 | 错误信息 | READY |
| CANCELLING | (立即) | "已取消" → "就绪，按 F9 说话（Ctrl+C 退出）" | READY |
| EDITING | Enter | 识别文本发送 | AGENT_RUNNING/READY |
| EDITING | Esc | "已取消" → "就绪，按 F9 说话..." | READY |
| EDITING | Ctrl+U | 清空缓冲区 | EDITING (继续) |
| EDITING | Backspace | 删除末字符 | EDITING (继续) |
| EDITING | 字符输入 | 替换/追加 | EDITING (继续) |

## UI 显示规范

### 识别编辑界面 (EDITING)

```
🎤 写一个hello world程序并运行
(Enter 发送 / Esc 取消 / Ctrl+U 清空)
```

- **第一行**: 识别文本（可编辑），绿色
- **第二行**: 操作提示，灰色 DIM

### 状态提示格式

| 状态 | 格式 |
|------|------|
| READY | 黄色，单行 |
| RECORDING | 黄色，单行 |
| PROCESSING | 黄色，单行 |
| CANCELLING | DIM "已取消" → READY 提示 |
| EDITING | 绿色文本 + DIM 提示，两行 |

## 键盘输入处理

| 按键 | 字节码 | 动作 |
|------|--------|------|
| Enter | 0x0d / 0x0a | 确认发送 |
| Esc | 0x1b | 取消 |
| Ctrl+U | 0x15 | 清空缓冲区 |
| Backspace | 0x7f / 0x08 | 删除光标前字符 |
| 可打印字符 | - | 在光标位置插入 |
| 左箭头 | ESC [ D / ESC O D | 光标左移 |
| 右箭头 | ESC [ C / ESC O C | 光标右移 |

**光标编辑行为（v2.1 更新）：**

- **始终插入**：输入字符始终在光标位置插入，不再有"首次输入替换"的反直觉行为
- **方向键支持**：支持两种格式的方向键序列
  - CSI 格式：`ESC [ A/B/C/D`（大多数终端）
  - SS3 格式：`ESC O A/B/C/D`（部分终端如 xterm）
- **移动后插入**：方向键移动光标后，后续输入自动插入到新位置（`hasEdited` 自动设为 true）

**示例场景：**

```
初始状态："你好，世界。" 光标在末尾（位置 6）
1. 按左箭头 → 光标移到位置 5（"。" 前）
2. 输入 "的" → 结果："你好，世界的。" （插入而非替换）

如果想替换整个文本，可先按 Ctrl+U 清空，再输入新内容。
```

## 实现要点

1. **状态转换延迟**: CANCELLING 状态需要短暂显示 "已取消" 后再显示 READY 提示
2. **光标覆盖**: 使用 `\r\x1b[K` 清除当前行，确保 READY 提示覆盖 "已取消"
3. **两行编辑界面**: 识别文本和操作提示分行显示，便于用户聚焦文本
4. **编辑模式光标**: 文本末尾应有光标，支持实时编辑反馈

## 测试覆盖

- `tests/promptEditRecognition.test.ts`: 键盘输入处理、两行渲染格式
- `tests/trigger/*.test.ts`: F9 触发、Esc 取消
- `tests/stt/*.test.ts`: STT 结果处理

## 键盘输入详细处理规则

### 取消触发条件

| 按键 | 字节序列 | 是否取消 | 说明 |
|------|---------|---------|------|
| Esc | `1b` | ✓ 取消 | 单字节 ESC |
| 上箭头 | `1b 5b 41` / `1b 4f 41` | ✗ 忽略 | ESC[A 或 ESC OA（SS3） |
| 下箭头 | `1b 5b 42` / `1b 4f 42` | ✗ 忽略 | ESC[B 或 ESC OB（SS3） |
| 左箭头 | `1b 5b 44` / `1b 4f 44` | ✗ 忽略 | ESC[D 或 ESC OD（SS3），光标左移 |
| 右箭头 | `1b 5b 43` / `1b 4f 43` | ✗ 忽略 | ESC[C 或 ESC OC（SS3），光标右移 |
| Home | `1b 5b 48` | ✗ 忽略 | ESC[H 序列 |
| End | `1b 5b 46` | ✗ 忽略 | ESC[F 序列 |
| F1-F12 | `1b 4f xx` | ✗ 忽略 | 功能键序列 |

**SS3 格式支持（v2.1 新增）：**

部分终端（如 xterm、某些 SSH 客户端）发送方向键时使用 SS3 格式（`ESC O A/B/C/D`），
而非常见的 CSI 格式（`ESC [ A/B/C/D`）。VoxBridge 现已同时支持两种格式。

### 实现要点

```typescript
// 只把单字节的 ESC (0x1b) 视为取消
// 方向键等 ESC 序列不应触发取消
if (chunk.length === 1 && chunk[0] === 0x1b) {
  return { buffer: "", action: "cancel", hasEdited: false };
}

// 同时支持 CSI (ESC [) 和 SS3 (ESC O) 格式的方向键
if (chunk.length >= 3 && chunk[0] === 0x1b && (chunk[1] === 0x5b || chunk[1] === 0x4f)) {
  const dir = chunk[2];
  if (dir === 0x44) { // 左箭头
    const newCursor = Math.max(0, currentCursor - 1);
    // 方向键操作后设置 hasEdited=true，后续输入会插入而非替换
    return { buffer, action: "continue", hasEdited: true, cursor: newCursor };
  }
  // ...
}

// 输入字符：始终在光标位置插入（移除"首次替换"行为）
const newBuffer = buffer.slice(0, currentCursor) + printable + buffer.slice(currentCursor);
const newCursor = currentCursor + printable.length;
return { buffer: newBuffer, action: "continue", hasEdited: true, cursor: newCursor };
```

## 历史记录显示

- 保留最近 5 条用户输入的提示词
- Agent 执行完成后显示历史记录
- 使用灰色 DIM 样式显示历史提示词

## 状态行覆盖规则

- "已取消"显示在状态行（不换行）
- 500ms 后被就绪提示覆盖（同一行）
- 使用 `\r\x1b[K` 清除当前行后覆盖
