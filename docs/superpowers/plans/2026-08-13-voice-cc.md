# Voice-CC 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用本机麦克风语音控制 Claude Code CLI 进行编码：按住/切换 F9 说话 → 本地 Whisper 识别 → 经 Claude Agent SDK 送入持久会话 → 流式显示到终端。

**Architecture:** Node.js 主进程（TypeScript）持有 Agent SDK 会话与终端 UI，通过 stdio JSONL 与一个 Python worker（sounddevice 录音 + faster-whisper 识别）通信。语音管道在 Python 侧闭环，Node 只拿到最终文本。

**Tech Stack:** Node 24 + TypeScript + `@anthropic-ai/claude-agent-sdk`；Python 3.12 + `faster-whisper` + `sounddevice` + `numpy`；测试：Node `node:test`（经 tsx 加载）、Python `pytest`。

## Global Constraints

- 平台 Windows 11；Node ≥ 20（本机 v24.13.1）；Python 3.12（`winget install Python.Python.3.12`）。
- Whisper 模型 `large-v3`，文件存放 `D:\Models\faster-whisper-large-v3`（snapshot_download 的 `local_dir`）。
- Agent SDK 版本：`@anthropic-ai/claude-agent-sdk@^0.3.x`。**环境变量继承：`options.env` 整体替换 process.env，必须 `{ ...process.env, ...覆盖项 }`。**
- 模型档位必须全钉死（DeepSeek 单模型网关）：`ANTHROPIC_MODEL` + `ANTHROPIC_DEFAULT_OPUS/SONNET/HAIKU_MODEL`，值从 cwd 的 `.claude/settings.json` env 块读取。
- `systemPrompt: { type: "preset", preset: "claude_code" }`（保持与 CLI 行为一致）；`settingSources: ["user", "project"]`。
- 通信协议为 stdio JSONL，UTF-8，`\n` 结尾（类型见 Task 4 / Task 7）。
- STT 与触发模块可插拔：`stt.plugin`（一期 `whisper`）、`trigger.global`（true=全局热键 hold-to-talk，false=终端内 F9 切换）。
- 所有新增代码用中文注释说明业务意图，标识符/日志用英文。
- 每次任务结束提交一次 git（Task 1 先 `git init`）。

---

### Task 1: 项目脚手架与环境

**Files:**
- Create: `package.json`, `tsconfig.json`, `.gitignore`, `config.json`, `scripts/setup-env.ps1`, `scripts/download-model.py`, `README.md`

**Interfaces:**
- Produces: `config.json`（Task 3 读取的标准格式）、`scripts/setup-env.ps1`（装 Python + venv + pip 包）、`scripts/download-model.py`（下载模型到 `D:\Models\faster-whisper-large-v3`）、`package.json` scripts（`start`, `test`, `worker`）

- [ ] **Step 1: 初始化 npm 与 git**

```bash
cd /d/.../innovationResearch
git init
npm init -y
```

- [ ] **Step 2: 写 `.gitignore`**

```text
node_modules/
dist/
.venv/
__pycache__/
*.pyc
.pytest_cache/
config.local.json
*.log
```

- [ ] **Step 3: 写 `package.json` 关键字段并安装依赖**

package.json 手动编辑：`"type": "module"`，scripts：
```json
{
  "start": "node --import tsx src/main.ts",
  "test": "node --import tsx --test tests/*.test.ts",
  "worker": "python stt_worker/main.py"
}
```
运行：
```bash
npm install @anthropic-ai/claude-agent-sdk@^0.3.x node-global-key-listener
npm install -D typescript tsx @types/node
```
预期：两个包安装成功。若 `node-global-key-listener` 原生构建失败（Node 24 无预编译二进制），记录输出，不阻塞——全局热键是可选功能（`trigger.global`），Task 9 用终端切换触发兜底。

- [ ] **Step 4: 写 `tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "tests/**/*.ts"]
}
```

- [ ] **Step 5: 写 `scripts/setup-env.ps1`（Python 环境）**

```powershell
# 用法: powershell -ExecutionPolicy Bypass -File scripts/setup-env.ps1
if (-not (Get-Command python -ErrorAction SilentlyContinue)) {
  winget install -e --id Python.Python.3.12
}
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install faster-whisper sounddevice numpy pytest
Write-Host "Python 环境就绪: .\.venv\Scripts\python.exe"
```
在 PowerShell 运行该脚本。预期：`.venv\Scripts\python.exe` 存在，`pip show faster-whisper` 有输出。

- [ ] **Step 6: 写 `scripts/download-model.py` 并下载模型**

```python
"""下载 faster-whisper large-v3 到 D:\\Models\\faster-whisper-large-v3。"""
import os
from huggingface_hub import snapshot_download

MODEL_DIR = r"D:\Models\faster-whisper-large-v3"
os.makedirs(os.path.dirname(MODEL_DIR), exist_ok=True)
snapshot_download(
    "Systran/faster-whisper-large-v3",
    local_dir=MODEL_DIR,
    local_dir_use_symlinks=False,
)
print(f"模型就绪: {MODEL_DIR}")
```
运行：`.\.venv\Scripts\python.exe scripts\download-model.py`
预期：`D:\Models\faster-whisper-large-v3` 下出现 `model.bin`、`config.json`、`tokenizer.json`、`vocabulary.txt`（约 1.5GB，可断点续传）。

- [ ] **Step 7: 写 `config.json` 模板**

```json
{
  "stt": { "plugin": "whisper", "model": "large-v3", "model_dir": "D:\\Models\\faster-whisper-large-v3", "language": "zh", "python_path": ".venv\\Scripts\\python.exe" },
  "trigger": { "mode": "push-to-talk", "key": "F9", "global": true },
  "agent": { "resume": true, "confirmDangerous": true, "systemPrompt": "" },
  "ui": { "showRecognition": true }
}
```

- [ ] **Step 8: 提交**

```bash
git add -A
git commit -m "chore: scaffold voice-cc project with env setup scripts"
```

---

### Task 2: Agent SDK 冒烟测试（最高风险先行）

验证整个控制通道在 DeepSeek 网关下可用，再铺开其余实现。

**Files:**
- Create: `scripts/smoke-session.ts`, `src/env.ts`

**Interfaces:**
- Produces: `src/env.ts` 导出 `readSettingsEnv(cwd: string): Record<string, string>`（Task 8 会话管理器复用）

- [ ] **Step 1: 写 `src/env.ts`（读 cwd + 全局 settings 的 env 块）**

```ts
import { readFileSync } from "node:fs";
import { join } from "node:path";
import { homedir } from "node:os";

function readEnvFrom(file: string): Record<string, string> {
  try {
    const raw = JSON.parse(readFileSync(file, "utf8"));
    return (raw.env ?? {}) as Record<string, string>;
  } catch {
    return {};
  }
}

/** 合并 cwd/.claude/settings.json 与 ~/.claude/settings.json 的 env 块，项目优先。 */
export function readSettingsEnv(cwd: string): Record<string, string> {
  const user = readEnvFrom(join(homedir(), ".claude", "settings.json"));
  const project = readEnvFrom(join(cwd, ".claude", "settings.json"));
  return { ...user, ...project };
}
```

- [ ] **Step 2: 写 `scripts/smoke-session.ts`**

```ts
import { query } from "@anthropic-ai/claude-agent-sdk";
import { readSettingsEnv } from "../src/env.js";

const env = { ...process.env, ...readSettingsEnv(process.cwd()) };
console.log("用 base URL:", env.ANTHROPIC_BASE_URL, "模型:", env.ANTHROPIC_MODEL);

const result = query({
  prompt: "只回复四个字：连接成功",
  options: {
    includePartialMessages: true,
    env,
    systemPrompt: { type: "preset", preset: "claude_code" },
    settingSources: ["user", "project"],
    permissionMode: "bypassPermissions",
  },
});

let sessionId: string | undefined;
for await (const msg of result) {
  if (msg.type === "system" && msg.subtype === "init") {
    sessionId = msg.session_id;
  } else if (msg.type === "stream_event") {
    const e = msg.event;
    if (e.type === "content_block_delta" && e.delta.type === "text_delta") {
      process.stdout.write(e.delta.text);
    }
  } else if (msg.type === "result") {
    if (msg.subtype === "success") {
      console.log("\n[OK] 会话ID:", msg.session_id ?? sessionId);
      process.exit(0);
    } else {
      console.error("\n[FAIL]", msg.is_error, (msg.errors ?? []).join("; "));
      process.exit(1);
    }
  }
}
```

- [ ] **Step 3: 运行冒烟测试**

```bash
node --import tsx scripts/smoke-session.ts
```
预期：终端流式打印「连接成功」，然后 `[OK] 会话ID: ...`，退出码 0。
若失败（404/400/超时）：检查（a）`ANTHROPIC_BASE_URL` 不含 `/v1` 后缀；（b）env 里三个 `ANTHROPIC_DEFAULT_*` 档位已钉死（复制 `.claude/settings.json` 的 env 块）；（c）`ANTHROPIC_AUTH_TOKEN` 正确。修复后重跑。**此任务必须通过才继续。**

- [ ] **Step 4: 提交**

```bash
git add -A
git commit -m "feat: verify Agent SDK works against DeepSeek gateway (smoke test)"
```

---

### Task 3: 配置加载器（TDD）

**Files:**
- Create: `src/config.ts`, `tests/config.test.ts`

**Interfaces:**
- Produces: `loadConfig(path: string): Config`（Task 9 主进程入口调用）
```ts
export interface Config {
  stt: { plugin: "whisper"; model: string; model_dir: string; language: string; python_path: string };
  trigger: { mode: "push-to-talk"; key: string; global: boolean };
  agent: { resume: boolean; confirmDangerous: boolean; systemPrompt: string };
  ui: { showRecognition: boolean };
}
```

- [ ] **Step 1: 写失败测试**

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { mkdtempSync, writeFileSync, rmSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { loadConfig } from "../src/config.js";

function withConfig(obj: unknown, fn: (p: string) => void) {
  const dir = mkdtempSync(join(tmpdir(), "vcc-"));
  const p = join(dir, "config.json");
  writeFileSync(p, JSON.stringify(obj));
  try { fn(p); } finally { rmSync(dir, { recursive: true, force: true }); }
}

test("解析合法配置", () => {
  withConfig({
    stt: { model: "large-v3", model_dir: "D:/Models/x", language: "zh", python_path: "py" },
    trigger: { key: "F9", global: true },
  }, (p) => {
    const c = loadConfig(p);
    assert.equal(c.stt.plugin, "whisper");
    assert.equal(c.stt.model, "large-v3");
    assert.equal(c.trigger.key, "F9");
    assert.equal(c.agent.resume, true); // 缺省
  });
});

test("非法 JSON 报错", () => {
  const dir = mkdtempSync(join(tmpdir(), "vcc-"));
  const p = join(dir, "config.json");
  writeFileSync(p, "{ not json");
  try { assert.throws(() => loadConfig(p)); } finally { rmSync(dir, { recursive: true, force: true }); }
});
```

- [ ] **Step 2: 运行确认失败**

Run: `node --import tsx --test tests/config.test.ts`
预期：FAIL（`loadConfig is not a function`）。

- [ ] **Step 3: 实现 `src/config.ts`**

```ts
import { readFileSync } from "node:fs";

export interface Config {
  stt: { plugin: "whisper"; model: string; model_dir: string; language: string; python_path: string };
  trigger: { mode: "push-to-talk"; key: string; global: boolean };
  agent: { resume: boolean; confirmDangerous: boolean; systemPrompt: string };
  ui: { showRecognition: boolean };
}

const DEFAULTS: Config = {
  stt: { plugin: "whisper", model: "large-v3", model_dir: "D:\\Models\\faster-whisper-large-v3", language: "zh", python_path: ".venv\\Scripts\\python.exe" },
  trigger: { mode: "push-to-talk", key: "F9", global: true },
  agent: { resume: true, confirmDangerous: true, systemPrompt: "" },
  ui: { showRecognition: true },
};

export function loadConfig(path: string): Config {
  const raw = JSON.parse(readFileSync(path, "utf8"));
  return {
    stt: { ...DEFAULTS.stt, ...(raw.stt ?? {}) },
    trigger: { ...DEFAULTS.trigger, ...(raw.trigger ?? {}) },
    agent: { ...DEFAULTS.agent, ...(raw.agent ?? {}) },
    ui: { ...DEFAULTS.ui, ...(raw.ui ?? {}) },
  };
}
```

- [ ] **Step 4: 运行确认通过**

Run: `node --import tsx --test tests/config.test.ts`
预期：PASS（2 个测试）。

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat: config loader with defaults and validation"
```

---

### Task 4: Python STT worker 骨架与协议（TDD）

**Files:**
- Create: `stt_worker/protocol.py`, `stt_worker/main.py`, `stt_worker/__init__.py`, `tests/python/test_protocol.py`, `tests/python/__init__.py`

**Interfaces:**
- Consumes: 无
- Produces: `stt_worker/main.py`（Node spawn 入口，argv: `--model <id> --model-dir <dir> --language <lang>`，读 stdin JSONL，写 stdout JSONL）；`stt_worker/protocol.py` 的 `encode(msg) -> str` / `decode(line) -> dict`

协议定义（本任务 + Task 7 一致）：
```
Node→Python:  {"type":"start"}  {"type":"stop"}  {"type":"quit"}
Python→Node:  {"type":"ready"}
              {"type":"recording"}
              {"type":"result","text":"...","duration_ms":1234}
              {"type":"noise"}
              {"type":"error","message":"..."}
```

- [ ] **Step 1: 写失败测试 `tests/python/test_protocol.py`**

```python
import pytest
from stt_worker.protocol import encode, decode


def test_encode_result():
    assert encode({"type": "result", "text": "改一下", "duration_ms": 1200}) == \
        '{"type": "result", "text": "改一下", "duration_ms": 1200}\n'


def test_decode_roundtrip():
    assert decode('{"type": "start"}\n') == {"type": "start"}


def test_decode_invalid_raises():
    with pytest.raises(ValueError):
        decode("not-json\n")
```

- [ ] **Step 2: 运行确认失败**

Run: `.\.venv\Scripts\python.exe -m pytest tests/python/test_protocol.py -v`
预期：FAIL（module not found）。

- [ ] **Step 3: 实现 `stt_worker/protocol.py`**

```python
"""stdio JSONL 编解码。"""
import json


def encode(msg: dict) -> str:
    """序列化为单行 JSON + 换行，ensure_ascii=False 保证中文可读。"""
    return json.dumps(msg, ensure_ascii=False) + "\n"


def decode(line: str) -> dict:
    """解析一行 JSON。非法输入抛 ValueError。"""
    try:
        return json.loads(line)
    except json.JSONDecodeError as e:
        raise ValueError(f"bad JSONL: {line!r}") from e
```

- [ ] **Step 4: 实现 `stt_worker/main.py` 骨架（协议循环，暂用假识别）**

```python
"""STT worker 入口：读 stdin JSONL，控制录音与识别，写 stdout JSONL。"""
import argparse
import sys

from stt_worker.protocol import decode, encode


def emit(msg: dict) -> None:
    """向 stdout 发送一条 JSONL 消息并立即冲刷。"""
    sys.stdout.write(encode(msg))
    sys.stdout.flush()


def main() -> None:
    parser = argparse.ArgumentParser()
    parser.add_argument("--model", default="large-v3")
    parser.add_argument("--model-dir", default=r"D:\Models\faster-whisper-large-v3")
    parser.add_argument("--language", default="zh")
    args = parser.parse_args()

    # 骨架阶段：用占位实现，Task 6 替换为真实录音 + Whisper。
    emit({"type": "ready"})
    recording = False
    for line in sys.stdin:
        line = line.strip()
        if not line:
            continue
        msg = decode(line)
        if msg["type"] == "start":
            recording = True
            emit({"type": "recording"})
        elif msg["type"] == "stop":
            recording = False
            emit({"type": "noise"})  # 占位：骨架阶段恒回 noise
        elif msg["type"] == "quit":
            break


if __name__ == "__main__":
    main()
```

- [ ] **Step 5: 写 `tests/python/test_worker_protocol.py`（端到端协议测试，替换 stdin/stdout）**

```python
import io
from unittest import mock

from stt_worker import main


def test_ready_then_noise_on_stop():
    in_buf = io.StringIO('{"type": "start"}\n{"type": "stop"}\n{"type": "quit"}\n')
    out = io.StringIO()
    with mock.patch.object(main.sys, "stdin", in_buf), \
         mock.patch.object(main.sys, "stdout", out):
        main.main()
    lines = out.getvalue().strip().splitlines()
    assert lines[0] == '{"type": "ready"}'
    assert any("noise" in l for l in lines)
```

- [ ] **Step 6: 运行全部 Python 测试确认通过**

Run: `.\.venv\Scripts\python.exe -m pytest tests/python -v`
预期：PASS（4 个测试）。

- [ ] **Step 7: 提交**

```bash
git add -A
git commit -m "feat: python stt worker skeleton with JSONL protocol"
```

---

### Task 5: Python 录音与端点检测（TDD）

**Files:**
- Create: `stt_worker/recorder.py`, `stt_worker/vad.py`, `tests/python/test_vad.py`

**Interfaces:**
- Consumes: 无
- Produces: `Recorder`（`start()` / `stop() -> np.ndarray` float32 mono 16kHz）；`has_voice(audio: np.ndarray, threshold: float) -> bool`；`is_silence(audio: np.ndarray) -> bool`（默认阈值）

- [ ] **Step 1: 写失败测试 `tests/python/test_vad.py`**

```python
import numpy as np
import pytest
from stt_worker.vad import is_silence


def test_silence_detects_zeros():
    assert is_silence(np.zeros(16000, dtype="float32"))


def test_loud_noise_is_not_silence():
    rng = np.random.default_rng(42)
    audio = rng.standard_normal(16000).astype("float32") * 0.5
    assert not is_silence(audio)


def test_short_silence_less_than_200ms_is_ignored():
    # 200ms 以下静音不判静音（防误杀词间停顿）
    assert not is_silence(np.zeros(1600, dtype="float32"))


def test_very_short_loud_burst_is_noise_like():
    # 噪声突刺（< 150ms）能量高但时长不足 → 视为无有效语音
    rng = np.random.default_rng(7)
    burst = rng.standard_normal(1200).astype("float32") * 0.9
    from stt_worker.vad import has_voice
    assert not has_voice(burst)
```

- [ ] **Step 2: 运行确认失败**

Run: `.\.venv\Scripts\python.exe -m pytest tests/python/test_vad.py -v`
预期：FAIL（import 错误）。

- [ ] **Step 3: 实现 `stt_worker/vad.py`**

```python
"""基于能量的端点检测：区分静音 / 噪声 / 有效语音。"""
import numpy as np

SAMPLE_RATE = 16000
SILENCE_RMS = 1e-4        # 低于此 RMS 视为静音
NOISE_MAX_RMS = 1e-2      # 超过此 RMS 且持续足够的时长才可能是有声
MIN_VOICE_MS = 200        # 有效语音最短持续时长


def rms(audio: np.ndarray) -> float:
    if audio.size == 0:
        return 0.0
    return float(np.sqrt(np.mean(audio**2)))


def is_silence(audio: np.ndarray) -> bool:
    """能量低于阈值且时长足够长（>=200ms）才算静音。"""
    if audio.size < int(SAMPLE_RATE * MIN_VOICE_MS / 1000):
        return False
    return rms(audio) < SILENCE_RMS


def has_voice(audio: np.ndarray) -> bool:
    """能量高于噪声上限且时长足够。"""
    if audio.size < int(SAMPLE_RATE * MIN_VOICE_MS / 1000):
        return False
    return rms(audio) > NOISE_MAX_RMS
```

- [ ] **Step 4: 实现 `stt_worker/recorder.py`**

```python
"""sounddevice 流式录音：回调累积 PCM 帧，stop 时拼接返回。"""
import numpy as np
import sounddevice as sd

from stt_worker.vad import SAMPLE_RATE


class Recorder:
    def __init__(self, samplerate: int = SAMPLE_RATE, channels: int = 1):
        self.samplerate = samplerate
        self.channels = channels
        self._frames: list[np.ndarray] = []
        self._stream: sd.InputStream | None = None

    def _callback(self, indata, frames, time, status) -> None:
        self._frames.append(indata.copy())

    def start(self) -> None:
        self._frames = []
        self._stream = sd.InputStream(
            samplerate=self.samplerate,
            channels=self.channels,
            dtype="float32",
            callback=self._callback,
        )
        self._stream.start()

    def stop(self) -> np.ndarray:
        """停止录音，返回 float32 单声道音频（长度 0 表示无帧）。"""
        if self._stream is not None:
            self._stream.stop()
            self._stream.close()
            self._stream = None
        if not self._frames:
            return np.zeros(0, dtype="float32")
        return np.concatenate(self._frames).reshape(-1, self.channels).mean(axis=1)
```

- [ ] **Step 5: 运行确认通过**

Run: `.\.venv\Scripts\python.exe -m pytest tests/python/test_vad.py -v`
预期：PASS（4 个测试）。

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat: python recorder and energy-based endpoint detection"
```

---

### Task 6: Python Whisper 识别引擎与完整 worker

**Files:**
- Create: `stt_worker/whisper_engine.py`, `tests/python/test_whisper_integration.py`

**Interfaces:**
- Consumes: `stt_worker/recorder.py` 的 `Recorder`、`stt_worker/vad.py` 的 `has_voice` / `is_silence`
- Produces: `WhisperEngine`（`load()` 一次加载；`transcribe(audio: np.ndarray) -> tuple[str, int]` 返回 `(text, duration_ms)`）；`main.py` 集成：stop 时 `has_voice` 为 False → `noise`，否则转写。

- [ ] **Step 1: 实现 `stt_worker/whisper_engine.py`**

```python
"""faster-whisper 封装：一次性加载模型，逐次转写。"""
import time
from typing import Tuple

import numpy as np
from faster_whisper import WhisperModel


class WhisperEngine:
    def __init__(self, model_dir: str, device: str = "cpu", compute_type: str = "int8"):
        self._model: WhisperModel | None = None
        self.model_dir = model_dir
        self.device = device
        self.compute_type = compute_type

    def load(self) -> None:
        """加载模型（首次会占用数秒与数 GB 内存，只调一次）。"""
        self._model = WhisperModel(
            self.model_dir, device=self.device, compute_type=self.compute_type
        )

    def transcribe(self, audio: np.ndarray, language: str = "zh") -> Tuple[str, int]:
        """转写音频，返回 (文本, 时长毫秒)。无语音返回 ("", 0)。"""
        if self._model is None:
            raise RuntimeError("WhisperEngine.load() must be called first")
        start = time.monotonic()
        segments, _ = self._model.transcribe(audio, language=language, vad_filter=True)
        text = "".join(seg.text for seg in segments).strip()
        return text, int((time.monotonic() - start) * 1000)
```

- [ ] **Step 2: 集成进 `stt_worker/main.py`（替换占位识别）**

```python
    args = parser.parse_args()
    engine = WhisperEngine(args.model_dir)
    engine.load()
    emit({"type": "ready"})
    recording = False
    for line in sys.stdin:
        line = line.strip()
        if not line:
            continue
        msg = decode(line)
        if msg["type"] == "start":
            recording = True
            emit({"type": "recording"})
        elif msg["type"] == "stop":
            recording = False
            if not has_voice(_current_audio) and not _has_valid_audio:
                emit({"type": "noise"})
            else:
                text, duration_ms = engine.transcribe(_current_audio, language=args.language)
                if text:
                    emit({"type": "result", "text": text, "duration_ms": duration_ms})
                else:
                    emit({"type": "noise"})
        elif msg["type"] == "quit":
            break
```
（真实实现中 `_current_audio` 由 `Recorder.stop()` 返回并先经 `has_voice` 判定；若 `has_voice` 为 False 直接 `noise`。）

- [ ] **Step 3: 写集成测试 `tests/python/test_whisper_integration.py`**

```python
import numpy as np
import pytest
from stt_worker.whisper_engine import WhisperEngine

MODEL_DIR = r"D:\Models\faster-whisper-large-v3"


@pytest.mark.integration
def test_silence_returns_empty():
    engine = WhisperEngine(MODEL_DIR)
    engine.load()
    text, _ = engine.transcribe(np.zeros(32000, dtype="float32"))  # 2s 静音
    assert text == ""


@pytest.mark.integration
def test_large_model_loaded():
    import os
    assert os.path.isfile(os.path.join(MODEL_DIR, "model.bin"))
```
运行：`.\.venv\Scripts\python.exe -m pytest tests/python -m integration -v`
预期：PASS。此任务不做真实语音样本断言（无法本地合成中文语音），改用静音空结果 + 模型文件存在性验证；**真实语音验证放到 Task 10 端到端手动确认**。

- [ ] **Step 4: 手动冒烟 worker（真实麦克风）**

（Windows 下用 PowerShell：`@('{"type":"start"}','{"type":"stop"}','{"type":"quit"}') | .\.venv\Scripts\python.exe stt_worker\main.py`）对着麦克风说一句"帮我写一个 hello world"，预期 stdout 出现 `{"type":"result","text":"帮我写一个hello world"...}`。若识别为空，检查麦克风采样率/增益。此步失败不阻塞后续，留待 Task 10 联调。

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat: whisper engine integration into worker"
```

---

### Task 7: Node STT 客户端（TDD）

**Files:**
- Create: `src/stt/types.ts`, `src/stt/protocol.ts`, `src/stt/workerClient.ts`, `src/stt/index.ts`, `tests/sttProtocol.test.ts`

**Interfaces:**
- Consumes: `Config.stt`（Task 3）、协议定义（Task 4）
- Produces:
```ts
// src/stt/types.ts
export type SttCommand = { type: "start" } | { type: "stop" } | { type: "quit" };
export type SttEvent =
  | { type: "ready" }
  | { type: "recording" }
  | { type: "result"; text: string; duration_ms: number }
  | { type: "noise" }
  | { type: "error"; message: string };
export type SttResult =
  | { kind: "text"; text: string }
  | { kind: "noise" }
  | { kind: "error"; message: string };

export interface SttClient {
  start(): void;
  stop(): Promise<SttResult>;
  cancel(): void;
  quit(): Promise<void>;
}
export function createSttClient(stt: Config["stt"], cwd: string): SttClient;
```

- [ ] **Step 1: 写失败测试 `tests/sttProtocol.test.ts`（纯编解码 + mock worker 客户端）**

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { encodeCommand, parseEvent } from "../src/stt/protocol.js";
import { WorkerSttClient } from "../src/stt/workerClient.js";
import { EventEmitter } from "node:events";

test("encodeCommand 生成单行 JSON", () => {
  assert.equal(encodeCommand({ type: "start" }), '{"type":"start"}\n');
});

test("parseEvent 解析 result 行", () => {
  assert.deepEqual(parseEvent('{"type":"result","text":"你好","duration_ms":800}'), {
    type: "result", text: "你好", duration_ms: 800,
  });
});

test("WorkerSttClient 收到 result 后 stop 返回文本", async () => {
  const fakeStdout = new EventEmitter();
  const client = new WorkerSttClient(fakeStdout as any, { start: () => {}, stop: () => {}, quit: () => {} } as any);
  const pending = client.stop();
  (fakeStdout as any).emit("data", '{"type":"result","text":"改一下","duration_ms":900}\n');
  const r = await pending;
  assert.deepEqual(r, { kind: "text", text: "改一下" });
});

test("WorkerSttClient 收到 noise 后 stop 返回 noise", async () => {
  const fakeStdout = new EventEmitter();
  const client = new WorkerSttClient(fakeStdout as any, { start: () => {}, stop: () => {}, quit: () => {} } as any);
  const pending = client.stop();
  (fakeStdout as any).emit("data", '{"type":"noise"}\n');
  assert.deepEqual(await pending, { kind: "noise" });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `node --import tsx --test tests/sttProtocol.test.ts`
预期：FAIL（module not found）。

- [ ] **Step 3: 实现 `src/stt/types.ts`、`src/stt/protocol.ts`、`src/stt/workerClient.ts`、`src/stt/index.ts`**

```ts
// protocol.ts
import type { SttCommand, SttEvent } from "./types.js";
export function encodeCommand(cmd: SttCommand): string {
  return JSON.stringify(cmd) + "\n";
}
export function parseEvent(line: string): SttEvent {
  const e = JSON.parse(line);
  if (e.type === "result") return { type: "result", text: e.text, duration_ms: e.duration_ms };
  return e as SttEvent;
}
```

```ts
// workerClient.ts
import { spawn, ChildProcess } from "node:child_process";
import { Readable } from "node:stream";
import { createInterface } from "node:readline";
import { encodeCommand, parseEvent } from "./protocol.js";
import type { SttClient, SttCommand, SttEvent, SttResult } from "./types.js";
import type { Config } from "../config.js";

/** 通过 stdio JSONL 与 Python worker 通信。构造可注入 stdio 便于测试。 */
export class WorkerSttClient implements SttClient {
  private reader: AsyncIterableIterator<string>;
  private pending: { resolve: (r: SttResult) => void; reject: (e: Error) => void } | null = null;

  constructor(
    private stdout: Readable,
    private send: (cmd: SttCommand) => void,
  ) {
    this.reader = createInterface({ input: stdout as any, crlfDelay: Infinity })[Symbol.asyncIterator]();
    this.pump();
  }

  private async pump(): Promise<void> {
    for await (const line of this.reader) {
      if (!line.trim()) continue;
      const ev = parseEvent(line);
      this.dispatch(ev);
    }
  }

  private dispatch(ev: SttEvent): void {
    if (ev.type === "result" || ev.type === "noise" || ev.type === "error") {
      const p = this.pending;
      this.pending = null;
      if (!p) return;
      if (ev.type === "result") p.resolve({ kind: "text", text: ev.text });
      else if (ev.type === "noise") p.resolve({ kind: "noise" });
      else p.resolve({ kind: "error", message: ev.message });
    }
  }

  static spawnFor(stt: Config["stt"], cwd: string): WorkerSttClient {
    const child = spawn(resolvePython(stt.python_path, cwd), [
      "stt_worker/main.py", "--model", stt.model, "--model-dir", stt.model_dir, "--language", stt.language,
    ], { cwd });
    return new WorkerSttClient(child.stdout!, (cmd) => child.stdin!.write(encodeCommand(cmd)));
  }

  start(): void { this.send({ type: "start" }); }
  stop(): Promise<SttResult> {
    this.send({ type: "stop" });
    return new Promise((resolve, reject) => { this.pending = { resolve, reject }; });
  }
  cancel(): void { this.pending = null; }
  async quit(): Promise<void> { this.send({ type: "quit" }); }
}

function resolvePython(pythonPath: string, cwd: string): string {
  return require("node:path").isAbsolute(pythonPath) ? pythonPath : require("node:path").join(cwd, pythonPath);
}
```

```ts
// index.ts
import type { Config } from "../config.js";
import { WorkerSttClient } from "./workerClient.js";
import type { SttClient } from "./types.js";

export * from "./types.js";

export function createSttClient(stt: Config["stt"], cwd: string): SttClient {
  return WorkerSttClient.spawnFor(stt, cwd);
}
```

- [ ] **Step 4: 运行确认通过**

Run: `node --import tsx --test tests/sttProtocol.test.ts`
预期：PASS（4 个测试）。

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat: node stt worker client with jsonl protocol"
```

---

### Task 8: Agent 会话管理器（TDD，mock SDK）

**Files:**
- Create: `src/session/agentSession.ts`, `src/session/types.ts`, `tests/session.test.ts`

**Interfaces:**
- Consumes: `Config`（Task 3）、`readSettingsEnv`（Task 2）
- Produces:
```ts
export interface SessionCallbacks {
  onTextDelta(text: string): void;
  onToolStart(name: string): void;
  onStatus(status: "sending" | "idle" | "error"): void;
}
export interface AgentSession {
  send(prompt: string): Promise<{ ok: true; sessionId: string } | { ok: false; error: string }>;
  reset(): void;
}
export function createAgentSession(opts: {
  config: Config; cwd: string; callbacks: SessionCallbacks;
  queryImpl?: typeof query; // 可注入，默认从 SDK 导入
}): AgentSession;
```

- [ ] **Step 1: 写失败测试 `tests/session.test.ts`（用假 query 生成器）**

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { createAgentSession } from "../src/session/agentSession.js";

function fakeQuery(messages: any[]) {
  return async function* () {
    for (const m of messages) yield m;
  };
}

test("流式文本触发 onTextDelta，success 返回会话 id", async () => {
  const deltas: string[] = [];
  const session = createAgentSession({
    config: { stt: {} as any, trigger: {} as any, agent: { resume: true, confirmDangerous: true, systemPrompt: "" }, ui: {} as any },
    cwd: process.cwd(),
    callbacks: { onTextDelta: (t) => deltas.push(t), onToolStart: () => {}, onStatus: () => {} },
    queryImpl: fakeQuery([
      { type: "system", subtype: "init", session_id: "sess-1" },
      { type: "stream_event", event: { type: "content_block_delta", delta: { type: "text_delta", text: "你好" } } },
      { type: "stream_event", event: { type: "content_block_delta", delta: { type: "text_delta", text: "世界" } } },
      { type: "result", subtype: "success", session_id: "sess-1" },
    ]) as any,
  });
  const r = await session.send("你好");
  assert.equal(r.ok, true);
  assert.equal(deltas.join(""), "你好世界");
});

test("result 错误子类型返回失败", async () => {
  const session = createAgentSession({
    config: { stt: {} as any, trigger: {} as any, agent: { resume: true, confirmDangerous: true, systemPrompt: "" }, ui: {} as any },
    cwd: process.cwd(),
    callbacks: { onTextDelta: () => {}, onToolStart: () => {}, onStatus: () => {} },
    queryImpl: fakeQuery([
      { type: "result", subtype: "error_during_execution", is_error: true, errors: ["boom"] },
    ]) as any,
  });
  const r = await session.send("x");
  assert.equal(r.ok, false);
  if (!r.ok) assert.match(r.error, /boom/);
});
```

- [ ] **Step 2: 运行确认失败**

Run: `node --import tsx --test tests/session.test.ts`
预期：FAIL（module not found）。

- [ ] **Step 3: 实现 `src/session/types.ts` 与 `src/session/agentSession.ts`**

```ts
// types.ts
export interface SessionCallbacks {
  onTextDelta(text: string): void;
  onToolStart(name: string): void;
  onStatus(status: "sending" | "idle" | "error"): void;
}
export type SendResult = { ok: true; sessionId: string } | { ok: false; error: string };
export interface AgentSession {
  send(prompt: string): Promise<SendResult>;
  reset(): void;
}
```

```ts
// agentSession.ts
import { query as defaultQuery } from "@anthropic-ai/claude-agent-sdk";
import { readSettingsEnv } from "../env.js";
import type { Config } from "../config.js";
import type { AgentSession, SessionCallbacks } from "./types.js";

export interface AgentSessionOptions {
  config: Config;
  cwd: string;
  callbacks: SessionCallbacks;
  queryImpl?: typeof defaultQuery;
}

export function createAgentSession(opts: AgentSessionOptions): AgentSession {
  const { config, cwd, callbacks } = opts;
  const queryImpl = opts.queryImpl ?? defaultQuery;
  let lastSessionId: string | undefined;

  const baseOptions = {
    includePartialMessages: true,
    env: { ...process.env, ...readSettingsEnv(cwd) },
    permissionMode: "bypassPermissions" as const,
    systemPrompt: config.agent.systemPrompt
      ? config.agent.systemPrompt
      : { type: "preset" as const, preset: "claude_code" as const },
    settingSources: ["user", "project"] as const,
    cwd,
  };

  return {
    async send(prompt: string): Promise<SendResult> {
      callbacks.onStatus("sending");
      try {
        const result = queryImpl({
          prompt,
          options: {
            ...baseOptions,
            resume: config.agent.resume ? (lastSessionId ?? undefined) : undefined,
          },
        });
        let sessionId = lastSessionId;
        for await (const msg of result) {
          if (msg.type === "system" && msg.subtype === "init") {
            sessionId = msg.session_id;
          } else if (msg.type === "stream_event") {
            const e = msg.event;
            if (e.type === "content_block_delta" && e.delta.type === "text_delta") {
              callbacks.onTextDelta(e.delta.text);
            } else if (e.type === "content_block_start" && e.content_block.type === "tool_use") {
              callbacks.onToolStart(e.content_block.name);
            }
          } else if (msg.type === "result") {
            if (msg.subtype === "success") {
              lastSessionId = msg.session_id ?? sessionId;
              callbacks.onStatus("idle");
              return { ok: true, sessionId: lastSessionId };
            }
            callbacks.onStatus("error");
            return { ok: false, error: (msg.errors ?? []).join("; ") };
          }
        }
        callbacks.onStatus("error");
        return { ok: false, error: "unexpected end of stream" };
      } catch (err) {
        callbacks.onStatus("error");
        return { ok: false, error: err instanceof Error ? err.message : String(err) };
      }
    },
    reset(): void {
      lastSessionId = undefined;
    },
  };
}
```

- [ ] **Step 4: 运行确认通过**

Run: `node --import tsx --test tests/session.test.ts`
预期：PASS（2 个测试）。

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat: agent session manager wrapping agent sdk query loop"
```

---

### Task 9: 触发模块与终端 UI

**Files:**
- Create: `src/trigger/types.ts`, `src/trigger/terminalKeys.ts`, `src/trigger/globalHotkey.ts`, `src/trigger/index.ts`, `src/ui/console.ts`, `tests/terminalKeys.test.ts`

**Interfaces:**
- Consumes: `Config.trigger`（Task 3）
- Produces:
```ts
// trigger/types.ts
export interface TriggerCallbacks {
  onStartListening(): void;
  onStopListening(): void;
  onCancel(): void;
}
export interface Trigger { start(cb: TriggerCallbacks): void; stop(): void; }
export function createTrigger(trigger: Config["trigger"]): Trigger;
```
`terminalKeys.ts`：`feedTerminalInput(chunk: string): KeyAction[]`，`KeyAction = { kind: "toggle" } | { kind: "cancel" }`（F9 切换、Esc 取消）。

- [ ] **Step 1: 写失败测试 `tests/terminalKeys.test.ts`**

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { feedTerminalInput } from "../src/trigger/terminalKeys.js";

test("F9 转义序列触发 toggle", () => {
  assert.deepEqual(feedTerminalInput("\u001b[18~"), [{ kind: "toggle" }]);
});
test("Esc 触发 cancel", () => {
  assert.deepEqual(feedTerminalInput("\u001b"), [{ kind: "cancel" }]);
});
test("普通字符不触发动作", () => {
  assert.deepEqual(feedTerminalInput("hello"), []);
});
test("连续输入解析多个动作", () => {
  assert.deepEqual(feedTerminalInput("\u001b[18~\u001b"), [
    { kind: "toggle" }, { kind: "cancel" },
  ]);
});
```

- [ ] **Step 2: 运行确认失败**

Run: `node --import tsx --test tests/terminalKeys.test.ts`
预期：FAIL（module not found）。

- [ ] **Step 3: 实现 `src/trigger/terminalKeys.ts`**

```ts
export type KeyAction = { kind: "toggle" } | { kind: "cancel" };

/**
 * 解析终端原始输入中的按键序列。
 * F9 在 xterm 序列中为 ESC [ 1 8 ~，Esc 为单个 ESC(0x1b)。
 */
export function feedTerminalInput(chunk: string): KeyAction[] {
  const out: KeyAction[] = [];
  let i = 0;
  while (i < chunk.length) {
    if (chunk[i] === "\u001b") {
      if (chunk.startsWith("\u001b[18~", i)) {
        out.push({ kind: "toggle" });
        i += 6;
      } else {
        out.push({ kind: "cancel" }); // 孤立的 ESC 视为取消
        i += 1;
      }
    } else {
      i += 1;
    }
  }
  return out;
}
```

- [ ] **Step 4: 实现 `src/trigger/index.ts`（两种触发，config.global 选择）**

```ts
import { GlobalKeyboardListener } from "node-global-key-listener";
import { createInterface } from "node:readline";
import { feedTerminalInput } from "./terminalKeys.js";
import type { Trigger, TriggerCallbacks } from "./types.js";
import type { Config } from "../config.js";

export * from "./types.js";

/** 全局热键 hold-to-talk：任意应用按下 F9 开始，松开结束。依赖 node-global-key-listener 原生模块。 */
export function createGlobalTrigger(key: string): Trigger {
  let cb: TriggerCallbacks | null = null;
  let listener: GlobalKeyboardListener | null = null;
  return {
    start(c: TriggerCallbacks) {
      cb = c;
      listener = new GlobalKeyboardListener();
      listener.addListener((e: any, down: boolean) => {
        if (e.name !== key) return;
        if (down) cb!.onStartListening();
        else cb!.onStopListening();
      });
    },
    stop() { listener?.kill(); listener = null; },
  };
}

/** 终端内 F9 切换：按下 F9 开始录音，再按结束；Esc 取消。无原生依赖，最可靠。 */
export function createTerminalTrigger(): Trigger {
  let cb: TriggerCallbacks | null = null;
  let rl: ReturnType<typeof createInterface> | null = null;
  let listening = false;
  return {
    start(c: TriggerCallbacks) {
      cb = c;
      process.stdin.setRawMode(true);
      rl = createInterface({ input: process.stdin, terminal: false });
      rl.on("line", (line) => {
        for (const action of feedTerminalInput(line)) {
          if (action.kind === "toggle") {
            listening = !listening;
            listening ? cb!.onStartListening() : cb!.onStopListening();
          } else {
            cb!.onCancel();
          }
        }
      });
    },
    stop() { rl?.close(); process.stdin.setRawMode(false); rl = null; },
  };
}

export function createTrigger(trigger: Config["trigger"]): Trigger {
  return trigger.global ? createGlobalTrigger(trigger.key) : createTerminalTrigger();
}
```

- [ ] **Step 5: 实现 `src/ui/console.ts`（ANSI 辅助）**

```ts
const RESET = "\x1b[0m";
const GREEN = "\x1b[32m";
const RED = "\x1b[31m";
const DIM = "\x1b[2m";
const YELLOW = "\x1b[33m";

export function printStatus(text: string): void { process.stdout.write(`\r${YELLOW}${text}${RESET}\n`); }
export function printRecognition(text: string): void { process.stdout.write(`\r${GREEN}🎤 ${text}${RESET}\n`); }
export function printAssistantDelta(text: string): void { process.stdout.write(text); }
export function printToolLine(text: string): void { process.stdout.write(`\n${DIM}└ ${text}${RESET}\n`); }
export function printError(text: string): void { process.stdout.write(`\r${RED}✖ ${text}${RESET}\n`); }
export function clearStatusLine(): void { process.stdout.write("\r\x1b[K"); }
```

- [ ] **Step 6: 运行测试确认通过**

Run: `node --import tsx --test tests/terminalKeys.test.ts`
预期：PASS（4 个测试）。

- [ ] **Step 7: 提交**

```bash
git add -A
git commit -m "feat: pluggable trigger (global hotkey / terminal toggle) and console ui"
```

---

### Task 10: 主进程装配与端到端

**Files:**
- Create: `src/main.ts`, `scripts/e2e.ts`

**Interfaces:**
- Consumes: `loadConfig`、`createSttClient`、`createAgentSession`、`createTrigger`、`ui`
- Produces: 可运行应用 `npm start`；端到端验证脚本

- [ ] **Step 1: 实现 `src/main.ts`**

```ts
import { loadConfig } from "./config.js";
import { createSttClient } from "./stt/index.js";
import { createAgentSession } from "./session/agentSession.js";
import { createTrigger } from "./trigger/index.js";
import * as ui from "./ui/console.js";

const config = loadConfig(process.argv[2] ?? "./config.json");
const stt = createSttClient(config.stt, process.cwd());
const session = createAgentSession({ config, cwd: process.cwd(), callbacks: {
  onTextDelta: (t) => ui.printAssistantDelta(t),
  onToolStart: (name) => ui.printToolLine(`▶ ${name}`),
  onStatus: (s) => { if (s === "error") ui.printError("agent 调用失败"); },
} });

async function handleStop() {
  ui.clearStatusLine();
  const result = await stt.stop();
  if (result.kind === "text") {
    ui.printRecognition(result.text);
    const r = await session.send(result.text);
    if (!r.ok) ui.printError(r.error);
    ui.printStatus("就绪，按 F9 说话");
  } else if (result.kind === "noise") {
    ui.printStatus("未识别到语音");
  } else {
    ui.printError(result.message);
  }
}

const trigger = createTrigger(config.trigger);
trigger.start({
  onStartListening: () => { stt.start(); ui.printStatus("🎙 录音中…（松开结束 / Esc 取消）"); },
  onStopListening: () => { void handleStop(); },
  onCancel: () => { stt.cancel(); ui.clearStatusLine(); ui.printStatus("已取消"); },
});

process.on("SIGINT", async () => {
  trigger.stop();
  await stt.quit();
  process.exit(0);
});
ui.printStatus(`就绪，按 ${config.trigger.key} 说话（Ctrl+C 退出）`);
```

- [ ] **Step 2: 实现 `scripts/e2e.ts`（模拟触发，验证全链路）**

```ts
import { loadConfig } from "../src/config.js";
import { createAgentSession } from "../src/session/agentSession.js";
import * as ui from "../src/ui/console.js";

// 端到端：不经过键盘，用一段文本直发 agent，验证 Agent SDK 会话链路。
// 语音链路在 Task 10 Step 3 用真实 app 验证。
const config = loadConfig("./config.json");
const session = createAgentSession({ config, cwd: process.cwd(), callbacks: {
  onTextDelta: (t) => process.stdout.write(t),
  onToolStart: (n) => ui.printToolLine(`▶ ${n}`),
  onStatus: () => {},
} });
const prompt = process.argv[2] ?? "只回复：ok";
const r = await session.send(prompt);
console.log("\n" + (r.ok ? `[OK] ${r.sessionId}` : `[FAIL] ${r.error}`));
process.exit(r.ok ? 0 : 1);
```

- [ ] **Step 3: 端到端手动验证**

```bash
npm start
```
验证步骤：
1. 终端显示「就绪，按 F9 说话」。
2. 按住 F9 对着麦克风说"在当前目录新建一个 hello.txt，内容是 hello"，松开。
3. 终端绿色回显识别文本，随后流式显示 agent 处理过程，最终创建文件。
4. 说第二句（如"读一下 hello.txt"），确认 agent 记得上一轮（会话恢复生效）。
5. 按 Esc 验证取消不发送。

预期：全部通过。若语音识别不准，调整麦克风距离/增益；若 agent 报错，回到 Task 2 冒烟排查环境变量。

- [ ] **Step 4: 提交**

```bash
git add -A
git commit -m "feat: wire main entry and end-to-end verification"
```

---

## 自查结论

- **Spec 覆盖**：交互模式（Task 10 装配）、STT 可插拔（Task 7 `stt.plugin` + 注册表）、Whisper large-v3 路径 D:\Models（Task 1/6）、Agent SDK 会话 + 流式 + resume（Task 8）、push-to-talk F9（Task 9）、无 TTS（设计即无）、错误处理（各任务内嵌 + Task 10 状态机）、测试（Task 3/4/5/7/8/9 TDD）。
- **占位符**：无 TBD；Task 4 的占位识别在 Task 6 明确替换。
- **类型一致性**：`SttResult`/`SttEvent`/`SttCommand` 在 Task 4（Python）与 Task 7（Node）定义一致；`SessionCallbacks` 在 Task 8 定义、Task 10 消费一致；`Config` 在 Task 3 定义、Task 7/8/9 消费一致。
