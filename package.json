{
  "name": "voice-cc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Voice-controlled Claude Code CLI: push-to-talk speech-to-text frontend for Claude Code.",
  "scripts": {
    "start": "node --import tsx src/main.ts",
    "test": "node --import tsx --test tests/*.test.ts",
    "worker": "python stt_worker/main.py"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.231",
    "node-global-key-listener": "^0.3.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
